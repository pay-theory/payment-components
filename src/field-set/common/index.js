import * as data from './data'
import * as dom from './dom'
import * as message from './message'
import * as network from './network'

const common = { ...data, ...dom, ...message, ...network }

export default common
